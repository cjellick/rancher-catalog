version: '2'
services:
  scheduler:
    image: cjellick/scheduler:v0.6.0-ip-scheduling-1
    command: scheduler
    labels:
        io.rancher.container.create_agent: "true"
        io.rancher.container.agent_service.scheduling: "true"
    logging:
      driver: json-file
      options:
        max-size: 25m
        max-file: '2'
